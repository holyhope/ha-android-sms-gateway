{
  "config": {
    "step": {
      "user": {
        "description": "Connect to the Android SMS Gateway app running in Local Server mode.",
        "data": {
          "url": "Local server URL (e.g. http://192.168.1.12:8080)",
          "username": "Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication"
    },
    "abort": {
      "already_configured": "This device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "Sending SMS is unaffected by these options.",
        "data": {
          "events": "Events",
          "url_mode": "Webhook URL"
        },
        "data_description": {
          "events": "Which android-sms-gateway events to register a webhook for. Selected events become available as automation triggers. system:ping also drives the Online/Battery device entities.",
          "url_mode": "internal: the direct LAN address (default, works for most installs). external: the public URL — only if the device can't reach Home Assistant's internal address. auto: internal if configured, else external. This choice isn't verified — an unreachable or auth-gated external URL will register successfully and then silently never deliver."
        }
      }
    }
  },
  "services": {
    "send_sms": {
      "name": "Send SMS",
      "description": "Send a text message via the Android SMS Gateway app.",
      "fields": {
        "message": {
          "name": "Message",
          "description": "The text message to send."
        },
        "phone_number": {
          "name": "Phone number",
          "description": "Recipient phone number, in E.164 format."
        }
      }
    }
  },
  "device_automation": {
    "trigger_type": {
      "sms:received": "SMS received",
      "sms:sent": "SMS sent",
      "sms:delivered": "SMS delivered",
      "sms:failed": "SMS failed",
      "sms:data-received": "Data SMS received",
      "mms:received": "MMS received",
      "mms:downloaded": "MMS downloaded",
      "system:ping": "Gateway ping"
    }
  }
}
